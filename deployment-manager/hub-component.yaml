---
version: 1
kind: component
meta:
  name: deplyment-manager
  brief: GCP Cloud Deployment Manager
  source:
    dir: ../../components/deployment-manager

requires:
- gcp

parameters:
- name: dns.domain
- name: cloud.region

outputs:
- name: component.deployment-manager.bucket
  fromTfVar: name

templates:
  files:
  - "*.template"
