apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: ${component.minio.storageClass}
provisioner: kubernetes.io/aws-ebs
parameters:
  type: ${component.minio.volumeType}
  encrypted: "true"
