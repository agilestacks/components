apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ${component.kubeflow.jupyterhub.ingress.name}
  namespace: ${component.kubeflow.namespace}
  labels:
    provider:  agilestacks.com
    project:   kubernetes
spec:
  rules:
  - host: ${component.kubeflow.jupyterhub.ingress.urlPrefix}.${component.ingress.ssoUrlPrefix}.${dns.domain}
    http:
      paths:
      - path: /
        backend:
          serviceName: ${component.kubeflow.jupyterhub.ingress.service}
          servicePort: 80
