apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ${component.kubeflow.tf.ingress.name}
  namespace: ${component.kubeflow.namespace}
  labels:
    provider:  agilestacks.com
    project:   kubernetes
spec:
  rules:
  - host: ${component.kubeflow.tf.ingress.urlPrefix}.${component.ingress.ssoFqdn}
    http:
      paths:
      - path: /tfjobs/ui
        backend:
          serviceName: ${component.kubeflow.tf.ingress.service}
          servicePort: 80
