apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-discovery
  labels:
    provider: agilestacks.com
    project: logs
    qualifier: elasticsearch
spec:
  selector:
    provider: agilestacks.com
    project: logs
    qualifier: elasticsearch
  ports:
  - name: transport
    port: 9300
    protocol: TCP
