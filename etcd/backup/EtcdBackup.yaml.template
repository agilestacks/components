apiVersion: "etcd.database.coreos.com/v1beta2"
kind: "EtcdBackup"
metadata:
  name: {{COMPONENT_NAME}}-{{TS}}
spec:
  clusterName: {{COMPONENT_NAME}}
  storageType: S3
  s3:
    s3Bucket: files.{{DOMAIN_NAME}}/backup/etcd/{{COMPONENT_NAME}}/{{TS}}
    awsSecret: {{AWS_SECRET_NAME}}
